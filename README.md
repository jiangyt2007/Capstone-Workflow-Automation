# Capstone-Workflow-Automation
Capstone Workflow Automation

To run the prototype, first import packages
```
pip install streamlit altair pandas
```

Then run the app via the terminal
```
streamlit run app.py
```
